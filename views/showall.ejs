
    <head>
        <%- include('./partials/head'); %>
    </head>

    <body>
        <section>
            <% if(typeof user=='undefined' ) { %>
                <%- include('./partials/navbar_not_logged'); %>
                    <% } else{ %>
                        <%- include('./partials/navbar_logged'); %>
                            <% } %>
        </section>
        <section>
    
            <%- include('./partials/search'); %>
    
        </section>
<div class="confainer-md d-flex justify-content-center align-items-center">
  <div class="row">
    <% if (useradd.length === 0) {%>
        <p class="mt-3 fs-5 text-center">Wygląda na to, że nie znaleźliśmy tego czego szukasz.<br>
        Sprawdź literówki, bądź użyj kategorii.<br>
        Ewentualnie byłem za leniwy i trzeba dodać ogłoszenie :)
        </p>
        <% } %>
  <% useradd.forEach(add => { %>
    <div class="col-md-4"></div>
    <div class="col-md-4 py-5">


        <div class="card">
            <img src="/uploads/<%= add.img %>" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title"><a href="/add/show/<%= add._id %>" class="text-decoration-none text-dark"><%= add.title %></a></h5>
              <hr>
              <p class="fs-6"><%= add.location %> - <%= add.date %></p>
              <p class="fs-5 fst-bolder"><%= add.price %> zł</p>
              <p class="fs-6"> Ilość wyświetleń: <%= add.views %></p>
            </div>
          </div>
          <% if(user.moderator === true){ %>
            <a class="link-danger" href="delete/<%= add._id %>">USUŃ OGŁOSZENIE</a>
        <% } else{ %>
          
        <% } %>
      </div>
      <div class="col-md-4"></div>
      <% }) %>
    </div>
    </div>
      <section>

        <%- include('./partials/footer'); %>

    </section>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>